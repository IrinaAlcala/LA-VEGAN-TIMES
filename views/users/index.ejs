<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="stylesheets/style.css" />
  <title>LA VEGAN TIMES</title>
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
  <link rel="stylesheet" href="/stylesheets/style.css" />
<script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>

</head>
<body>
  <nav>
    <div class="nav-wrapper brown">
      <div class="container"> 
          
     <a href="/restaurants" class="brand-logo right">LA VEGAN TIMES</a>
      <ul class="left hide-on-med-and-down">
        <li><a href="/restaurants">All Restaurants</a></li>
                       <li> 
                        <% if (user) { %>
                          
                        <li><a href="/logout"><i class="material-icons left">trending_flat</i>Log Out</a></li>
          
                        <% } else { %>
                          <li><a href="/auth/google"><i class="material-icons left">vpn_key</i>Login with Google</a></li>
                          
                   <% } %>
                    </ul>
                </div>
          
        </nav>
    </div>
  <div>
      <!-- only allows post if user is in db-->
    <% if (user) { %> 
        <!-- if loggedin user matches the user it's on -->
        <% if (user._id.equals(user && user._id)) { %> 
            <div>
                  <body>
           <div style="background-image:url(https://i.imgur.com/Me25sgi.png );width:650px;height:400px;">
            
           </div>
            
        
                <h2>Welcome, <%= user.name %>!</h2>
                <h3>Check out the list of vegan restaurants around Los Angeles and choose your favorites!</h3>
            
  

                
        <% }} %>
  </div>
</body>
</html>